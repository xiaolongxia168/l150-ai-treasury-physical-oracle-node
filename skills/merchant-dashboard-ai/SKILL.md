---
name: merchant-dashboard-ai
description: 商家后台智能助手 - 自动抓取抖音来客、美团开店宝全量数据，AI分析优化建议
metadata:
  {
    "openclaw": {
      "emoji": "🏪",
      "requires": {
        "bins": ["python3", "node"],
        "packages": ["playwright", "pandas", "schedule"]
      },
      "version": "1.0.0"
    }
  }
---

# 商家后台智能助手 (Merchant Dashboard AI)

自动化抓取抖音来客、美团开店宝商家后台的**全量数据**，并通过 AI 进行深度分析、学习优化、生成运营建议。

## 🎯 核心功能

### 1. **智能登录与会话保持**
- ✅ 自动登录抖音来客、美团开店宝
- ✅ Cookie 持久化，无需重复登录
- ✅ 会话过期自动检测和续期
- ✅ 支持验证码自动识别（OCR）

### 2. **全量数据自动抓取**
- 📊 **订单数据**: 全部订单、待发货、已完成、退款
- 💰 **财务数据**: 收入、支出、结算、提现记录
- 📦 **商品数据**: 在售、下架、库存、销量排行
- 👥 **客户数据**: 新客、复购、客户画像、消费习惯
- 📈 **营销数据**: 优惠券、活动效果、流量来源
- 💬 **评价数据**: 好评、差评、回复率、问题总结
- 🚚 **物流数据**: 配送时效、异常订单、退货率

### 3. **AI 自动分析**
- 🧠 **趋势分析**: 销量趋势、季节性波动、增长预测
- 🎯 **用户洞察**: 高价值客户识别、流失预警、复购推荐
- 📉 **问题诊断**: 差评根因分析、退货原因、优化建议
- 💡 **智能建议**: 定价策略、促销方案、库存优化
- 🔄 **自动学习**: 根据历史数据优化抓取策略和分析模型

### 4. **自动化报告**
- 📧 **每日简报**: 关键指标、异常提醒、待办事项
- 📊 **周报月报**: 经营分析、竞品对比、行动计划
- ⚠️ **实时预警**: 异常订单、库存告急、差评预警
- 📱 **多渠道推送**: 飞书、企业微信、邮件、短信

## 🚀 快速开始

### 第一步：安装依赖

```bash
# 进入技能目录
cd /Users/xiaolongxia/.openclaw/workspace/skills/merchant-dashboard-ai

# 安装 Python 依赖
pip3 install playwright pandas schedule requests beautifulsoup4 opencv-python

# 安装 Playwright 浏览器
playwright install chromium

# 安装 Node 依赖（可选，用于高级自动化）
npm install puppeteer-extra puppeteer-extra-plugin-stealth
```

### 第二步：配置商家后台

创建配置文件 `config.json`:

```json
{
  "platforms": {
    "douyin_laike": {
      "enabled": true,
      "name": "抖音来客",
      "login_url": "https://laike.douyin.com/",
      "username": "",
      "password": "",
      "cookie_file": "cookies/douyin_laike.json"
    },
    "meituan_kaidian": {
      "enabled": true,
      "name": "美团开店宝",
      "login_url": "https://e.dianping.com/",
      "username": "",
      "password": "",
      "cookie_file": "cookies/meituan_kaidian.json"
    }
  },
  "scraping": {
    "full_scan_interval": "daily",
    "full_scan_time": "02:00",
    "incremental_interval": 3600,
    "data_types": [
      "orders",
      "products",
      "customers",
      "finance",
      "marketing",
      "reviews",
      "logistics"
    ],
    "headless": true,
    "timeout": 30000
  },
  "ai_analysis": {
    "enabled": true,
    "model": "deepseek/deepseek-chat",
    "auto_insights": true,
    "learning_mode": true,
    "report_schedule": {
      "daily": "08:00",
      "weekly": "Monday 09:00",
      "monthly": "1st 10:00"
    }
  },
  "notifications": {
    "feishu_webhook": "",
    "email": "",
    "alert_threshold": {
      "low_stock": 10,
      "bad_review": 3.5,
      "refund_rate": 0.05
    }
  }
}
```

### 第三步：首次登录

```bash
# 启动交互式登录（会打开浏览器窗口）
python3 scripts/login_assistant.py --platform douyin_laike

# 登录完成后，Cookie 会自动保存
# 后续将自动使用保存的 Cookie，无需重复登录
```

### 第四步：启动自动抓取

```bash
# 方式1：手动触发全量抓取
python3 scripts/full_scraper.py --platforms all

# 方式2：启动定时任务（推荐）
python3 scripts/scheduler.py --daemon

# 方式3：集成到 OpenClaw cron
# 在 OpenClaw 中设置 cron 任务，每小时增量抓取，每天2点全量抓取
```

## 📋 核心脚本说明

### 1. `login_assistant.py` - 登录助手
交互式登录，处理验证码，保存 Cookie

```bash
python3 scripts/login_assistant.py --platform douyin_laike --headless false
```

### 2. `full_scraper.py` - 全量数据抓取
遍历所有页面，抓取全量历史数据

```bash
# 抓取所有平台所有数据
python3 scripts/full_scraper.py --platforms all --types all

# 只抓取订单和财务数据
python3 scripts/full_scraper.py --platforms douyin_laike --types orders,finance

# 抓取并保存为 CSV
python3 scripts/full_scraper.py --platforms all --output data/export_{date}.csv
```

### 3. `incremental_scraper.py` - 增量数据抓取
只抓取最新变化的数据（效率更高）

```bash
python3 scripts/incremental_scraper.py --since "2 hours ago"
```

### 4. `ai_analyzer.py` - AI 智能分析
对抓取的数据进行深度分析

```bash
# 生成今日分析报告
python3 scripts/ai_analyzer.py --report daily

# 分析特定问题
python3 scripts/ai_analyzer.py --focus "为什么退货率突然上升"

# 生成优化建议
python3 scripts/ai_analyzer.py --optimize pricing,inventory
```

### 5. `auto_pilot.py` - 自动驾驶模式
全自动运行：抓取 → 分析 → 报告 → 优化建议

```bash
# 启动自动驾驶（推荐）
python3 scripts/auto_pilot.py --interval 3600
```

## 🎨 使用示例

### 示例 1：每日自动化报告

```bash
# 每天早上8点自动生成并发送报告
python3 scripts/scheduler.py --task daily_report --time "08:00"
```

报告内容：
- 📊 昨日关键指标（GMV、订单量、客单价）
- 📈 与上周/上月对比
- ⚠️ 异常提醒（差评、退货、库存）
- 💡 今日优化建议

### 示例 2：实时异常监控

```bash
# 启动实时监控
python3 scripts/monitor.py --alerts all
```

自动预警：
- 🚨 差评预警（评分 < 3.5）
- 📦 库存告急（库存 < 10）
- 💸 异常退款（退款率 > 5%）
- 🚚 物流延迟（配送超时）

### 示例 3：智能定价建议

```bash
# 分析竞品定价，生成定价建议
python3 scripts/ai_analyzer.py --optimize pricing --competitors "竞品店铺ID"
```

AI 分析输出：
```
🎯 定价优化建议

【商品A】当前价格: ¥299
- 竞品均价: ¥279
- 建议价格: ¥289 (降价 ¥10)
- 预期影响: 销量提升 15%，利润率下降 2%

【商品B】当前价格: ¥199
- 竞品均价: ¥219
- 建议价格: 保持 ¥199 (价格优势明显)
- 预期影响: 继续保持市场竞争力
```

## 🧠 AI 学习与优化

### 自动学习机制

系统会自动学习并优化：

1. **抓取策略优化**
   - 识别高价值数据页面，优先抓取
   - 动态调整抓取频率
   - 避免重复抓取不变数据

2. **分析模型优化**
   - 根据历史数据训练预测模型
   - 识别业务模式和季节性规律
   - 提高预警准确度

3. **建议质量提升**
   - 跟踪建议实施效果
   - 优化建议生成算法
   - 个性化运营策略

### 手动触发学习

```bash
# 基于最近 30 天数据重新训练模型
python3 scripts/ai_analyzer.py --retrain --days 30

# 评估模型准确度
python3 scripts/ai_analyzer.py --evaluate
```

## 📊 数据存储

抓取的数据保存在 `data/` 目录：

```
data/
├── raw/                    # 原始数据
│   ├── douyin_laike/
│   │   ├── orders_2026-02-19.json
│   │   ├── products_2026-02-19.json
│   │   └── ...
│   └── meituan_kaidian/
│       └── ...
├── processed/              # 处理后的数据
│   ├── daily_stats.csv
│   ├── customer_profiles.csv
│   └── ...
├── analysis/               # AI 分析结果
│   ├── insights_2026-02-19.md
│   ├── predictions.json
│   └── ...
└── reports/                # 生成的报告
    ├── daily_2026-02-19.pdf
    └── weekly_2026-W07.pdf
```

## 🔐 安全与隐私

1. **Cookie 加密存储**
2. **敏感数据脱敏**
3. **本地数据存储（不上传云端）**
4. **定期清理过期数据**

## 🛠️ 故障排除

### 问题 1：登录失败

```bash
# 检查配置
python3 scripts/check_config.py

# 清除旧 Cookie 重新登录
rm -rf cookies/*
python3 scripts/login_assistant.py --platform douyin_laike --headless false
```

### 问题 2：抓取不完整

```bash
# 启用详细日志
python3 scripts/full_scraper.py --platforms all --verbose --headless false

# 检查日志
tail -f logs/scraper.log
```

### 问题 3：AI 分析慢

```bash
# 使用更快的模型
# 在 config.json 中设置: "model": "moonshot/kimi-k2.5"

# 或者减少分析数据量
python3 scripts/ai_analyzer.py --days 7  # 只分析最近 7 天
```

## 🚀 高级功能

### 1. 竞品监控
自动监控竞品店铺的价格、销量、评价

### 2. 自动回复
AI 自动回复客户咨询和差评

### 3. 库存预测
基于销售趋势预测库存需求

### 4. 营销自动化
自动创建和优化营销活动

## 📞 技术支持

遇到问题？
1. 查看日志：`tail -f logs/scraper.log`
2. 运行诊断：`python3 scripts/diagnose.py`
3. 提交 Issue 或联系开发者

## 📝 更新日志

- v1.0.0 (2026-02-19): 初始版本，支持抖音来客和美团开店宝

---

**让 AI 成为你的商家运营助手！** 🏪✨
